name: CI - Order Service

on:
  push:
    paths:
      - "apps/backend-api/order-service/**"
    branches: [ main ]

jobs:
  build:
    uses: DevOpsByOmer/Projects/.github/reusable/reusable-docker.yml@main
    with:
      service_name: order-service
      dockerfile_path: apps/backend-api/order-service/Dockerfile
      context: apps/backend-api/order-service
#uses: your-org/your-repo/.github/workflows/reusable-docker.yml@main

