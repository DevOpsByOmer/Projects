name: CI - Order Service

on:
  push:
    paths:
      - "apps/backend-api/order-service/**"
    branches: [ main ]

jobs:
  build:
    uses: DevOpsByOmer/Projects/.github/workflows/resuable/reusable-docker.yml@main
    with:
      service_name: order-service
      dockerfile_path: apps/order-service/Dockerfile
      context: apps/order-service
#uses: your-org/your-repo/.github/workflows/reusable-docker.yml@main

